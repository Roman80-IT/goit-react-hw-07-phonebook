export const getContacts = state => state.contactsOperation.contacts;

export const getFilter = state => state.contactsOperation.filter;

export const getName = state => state.contactsOperation.name;

export const getNumber = state => state.contactsOperation.number;
